{
  "name": "dreamscape-payments",
  "version": "1.0.0",
  "scripts": {
    "dev": "nodemon api/checkout.js",
    "start": "node api/checkout.js",
    "webhook": "stripe listen --forward-to localhost:4242/api/webhook",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "stripe": "^16.0.0",
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express-rate-limit": "^7.0.0",
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "nodemon": "^3.0.0",
    "prisma": "^5.0.0"
  }
}
